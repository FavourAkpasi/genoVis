import { DotsGrid } from '@/components/modules/case-study/components/finale';

/**
 * The final scene's animated visual. To preview a different option, swap the
 * component below for one of: PhyloTree, LineageRibbons, SpikeVirion, DotsGrid
 * (all exported from './index'). That is the only line to change.
 */
export const FinaleGraphic = () => <DotsGrid className="max-h-[360px]" />;

export default FinaleGraphic;
